rootProject.name = "archipelago-architecture"
if (!settings.hasProperty('buildPluginOnly') || settings.getProperty('buildPluginOnly') != 'true') {
    include("core")
    include("core:spring-lib")
    include("core:react-lib")
    include("core:pulumi")
}
include("build-plugin")

